'use strict';

var isDeviceIdSampled = require('../../utils/isDeviceIdSampled.js');

class EmbraceSDKFeaturesManager {
    constructor({ dynamicConfigManager, deviceId }){
        this._dynamicConfigManager = dynamicConfigManager;
        this._deviceId = deviceId;
    }
    isSDKEnabled() {
        const config = this._dynamicConfigManager.getConfig();
        return isDeviceIdSampled.isDeviceIdSampled(this._deviceId, config.samplingPct);
    }
}

exports.EmbraceSDKFeaturesManager = EmbraceSDKFeaturesManager;
//# sourceMappingURL=EmbraceSDKFeaturesManager.js.map
