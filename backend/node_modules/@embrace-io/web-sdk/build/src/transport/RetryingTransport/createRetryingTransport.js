'use strict';

var RetryingTransport = require('./RetryingTransport.js');

/**
 * Creates an Exporter Transport that retries on 'retryable' response.
 */ const createRetryingTransport = (options)=>new RetryingTransport.RetryingTransport(options.transport);

exports.createRetryingTransport = createRetryingTransport;
//# sourceMappingURL=createRetryingTransport.js.map
